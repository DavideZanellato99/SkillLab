services:
  db:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: skilllab_db
    volumes:
      - db_data:/var/lib/postgresql
    ports:
      # Exposed on the standard host port 5432 (user: postgres / pw: postgres).
      # The native Windows Postgres has been disabled to free this port.
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    environment:
      # Overrides the value from backend/.env: inside the compose network
      # Postgres is reachable at the "db" service name, not localhost.
      DATABASE_URL: postgresql+psycopg://postgres:postgres@db:5432/skilllab_db
    volumes:
      # Persist uploaded avatar images across rebuilds/restarts.
      - backend_static:/app/static
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      # Container listens on 8080 (unprivileged nginx); still exposed on host
      # port 3000.
      - "3000:8080"
    restart: unless-stopped

volumes:
  db_data:
  backend_static:
